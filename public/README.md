# Public Directory

You can upload your static assets (like `logo.png` or `logo.jpg`) here. 

Any files placed in this folder can be accessed directly from the root path (`/`). For example, if you upload `logo.png` here, it will be loaded as the system icon.
